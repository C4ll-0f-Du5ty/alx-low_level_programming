#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include "main.h"
/**
 * print_alphabet_x10 - prints a - z 10 times
 */

void print_alphabet_x10(void)
{
	int s, sh;

	for (s = 0; s < 10; s++)
	{
		sh = 'a'
	while (sh <= 'z')
	{
		_putchar(sh);
		sh++;
	}
	_putchar('\n');
	}
}
