#include "main.h"
#include <stdio.h>

/**
 * _puts - a function that prints a stirng
 *
 * @str: the string to be printed
 *
 * puts - is a function that prints a astring followed by anew line
 */

void _puts(char *str)
{
	puts(str);
}
